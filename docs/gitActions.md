# Automatización con GitHub Actions y MkDocs

El objetivo es implementar un ciclo de CI/CD para generar la documentación automáticamente tras cada push.

## 1. Configuración de MkDocs
Se instaló Python y MkDocs en el entorno local:

```bash
sudo dnf install -y python3 python3-pip
pip install --user mkdocs
```
